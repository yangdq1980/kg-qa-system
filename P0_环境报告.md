# P0 环境勘察与项目初始化 — 执行报告

**日期**: 2026-08-11  
**阶段**: P0  
**状态**: ✅ 完成（项目骨架生成，待目标机器验证）

---

## 一、执行说明

由于当前开发机器非目标工作站（目标工作站为另一台配置 NVIDIA GTX 5060 8GB、Win11、OpenClaw、Ollama 的机器），P0 阶段采用**项目骨架预生成**策略：

- 不在当前机器做连通性测试（Ollama / GLM / 向量库均在目标机器）
- 直接生成完整的项目目录结构、所有代码模板、配置文件、依赖清单
- 附带目标机器环境检查脚本和部署说明
- 用户将项目整体迁移（或 git clone）到目标工作站后，按检查清单验证环境

---

## 二、已生成文件清单

### 核心代码（15 个文件）

| 文件 | 说明 | 状态 |
|------|------|------|
| `app/main.py` | FastAPI 入口，含 /health 健康检查 | ✅ 完整可运行 |
| `app/models/schemas.py` | 全部 Pydantic 数据模型 | ✅ 完整定义 |
| `app/routers/graph.py` | 图谱 API 路由 | ✅ 结构完整，P2 填充 |
| `app/routers/search.py` | 搜索 API 路由 | ✅ 结构完整，P2 填充 |
| `app/routers/qa.py` | 问答 API 路由 | ✅ 结构完整，P3 填充 |
| `app/routers/admin.py` | 管理/抽取 API 路由 | ✅ 结构完整，P1 填充 |
| `app/services/graph_service.py` | NetworkX 图服务 | ✅ 部分实现（搜索/路径/布局），P1 补加载 |
| `app/services/rag_service.py` | RAG 向量检索服务 | ✅ 结构完整，P3 填充 |
| `app/services/entity_linker.py` | 实体链接服务 | ✅ 结构完整，P3 填充 |
| `app/services/qa_pipeline.py` | 融合问答流水线 | ✅ 核心流程完整，P3 调试 |
| `config/settings.py` | 全局配置（含本体 Schema） | ✅ 完整可配置 |

### 前端（3 个文件）

| 文件 | 说明 | 状态 |
|------|------|------|
| `app/static/index.html` | 单页应用骨架 | ✅ 完整可展示 |
| `app/static/css/style.css` | 样式（双栏布局） | ✅ 完整 |
| `app/static/js/app.js` | 前端逻辑（健康检查） | ✅ P0 功能可用 |

### 脚本工具（3 个文件）

| 文件 | 说明 | 状态 |
|------|------|------|
| `scripts/check_env.py` | 目标机器环境检查（6项检查） | ✅ 完整可运行 |
| `scripts/init_db.py` | SQLite 数据库初始化（6张表） | ✅ 完整可运行 |
| `scripts/extract_triples.py` | 三元组批量抽取模板 | ✅ 框架完整，P1 填充 |

### 配置与文档（5 个文件）

| 文件 | 说明 |
|------|------|
| `requirements.txt` | 15 个依赖包，指定版本 |
| `.env.example` | 环境变量模板 |
| `.gitignore` | Git 忽略规则（排除数据文件） |
| `README.md` | 项目说明 + 快速开始 |
| `需求建议书_v1.0.md` | 完整需求文档 |

---

## 三、目录结构

```
kg_qa_system/
├── app/
│   ├── __init__.py
│   ├── main.py                          ← 入口
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py                   ← 数据模型
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── graph.py                     ← P2
│   │   ├── search.py                    ← P2
│   │   ├── qa.py                        ← P3
│   │   └── admin.py                     ← P1
│   ├── services/
│   │   ├── __init__.py
│   │   ├── graph_service.py             ← 图服务
│   │   ├── rag_service.py               ← RAG
│   │   ├── entity_linker.py             ← 实体链接
│   │   └── qa_pipeline.py               ← 问答流水线
│   └── static/
│       ├── index.html                   ← 前端页面
│       ├── css/style.css
│       └── js/app.js
├── config/
│   └── settings.py                      ← 配置
├── data/
│   ├── docs/.gitkeep                    ← 放原始文档
│   ├── triples/.gitkeep
│   └── vector_store/.gitkeep            ← 向量库
├── db/                                  ← SQLite 数据库（运行后生成）
├── scripts/
│   ├── check_env.py                     ← 环境检查
│   ├── init_db.py                       ← 数据库初始化
│   └── extract_triples.py               ← 抽取脚本
├── tests/
├── requirements.txt
├── .env.example
├── .gitignore
├── README.md
├── 需求建议书_v1.0.md
└── P0_环境报告.md                       ← 本文件
```

---

## 四、目标机器待验证清单

迁移到目标工作站后，请按以下顺序验证：

### 4.1 必须通过（核心依赖）

| # | 检查项 | 检查命令/方法 | 预期结果 |
|---|--------|-------------|---------|
| 1 | Python 版本 | `python --version` | 3.10 ~ 3.13（避开 3.14） |
| 2 | 依赖安装 | `pip install -r requirements.txt` | 全部安装成功 |
| 3 | Ollama 服务 | `curl http://localhost:11434/api/tags` | 返回模型列表 |
| 4 | 问答模型 | `ollama list` | 至少有一个 7B 模型（deepseek-r1/qwen等） |
| 5 | 项目启动 | `uvicorn app.main:app --port 8000` | 无报错，浏览器可访问 |
| 6 | 健康检查 | 访问 `/health` | 返回各组件状态 |

### 4.2 建议配置（功能完整）

| # | 检查项 | 说明 |
|---|--------|------|
| 7 | GLM API Key | 在 `.env` 中配置，用于高质量三元组抽取 |
| 8 | 向量库路径 | 在 `config/settings.py` 或 `.env` 中配置 enterprise-rag 的向量库路径 |
| 9 | 嵌入模型一致性 | 确认 Ollama 中的嵌入模型与 enterprise-rag 建库时一致 |
| 10 | 文档放入 | 将 100+ 篇 Word/MD 放入 `data/docs/` 目录 |

### 4.3 一键检查

```bash
python scripts/check_env.py
```

该脚本会自动检查上述所有项，输出通过/未通过报告。

---

## 五、已知限制与后续计划

### 当前可运行的功能（P0）
- ✅ FastAPI 服务启动
- ✅ `/health` 健康检查（检测 Ollama / GLM / 向量库 / 数据库状态）
- ✅ 前端页面展示（系统状态面板）
- ✅ API 文档（`/docs`）
- ✅ 数据库初始化（`scripts/init_db.py`）

### 待开发（后续阶段）
- ⏳ P1: 文档解析 + GLM 三元组抽取 + 消歧 + 入库
- ⏳ P2: ECharts 图谱可视化 + 搜索 + 交互
- ⏳ P3: 实体链接 + 图谱RAG融合问答 + 前端联动
- ⏳ P4: 增量更新 + 性能优化 + 测试 + 部署手册

---

## 六、Git 初始化建议（可选）

如果使用 GitHub 管理代码和迁移：

```bash
cd kg_qa_system
git init
git add .
git commit -m "P0: 项目骨架初始化"
git branch -M main
git remote add origin <你的GitHub仓库地址>
git push -u origin main
```

目标工作站上：
```bash
git clone <你的GitHub仓库地址>
cd kg_qa_system
```

---

*P0 阶段完成。确认目标机器环境检查通过后，进入 P1 阶段。*
