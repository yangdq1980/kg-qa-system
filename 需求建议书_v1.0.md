# 本地企业知识图谱与智能问答系统 — 需求建议书

**版本**：v1.0  
**日期**：2026-08-11  
**状态**：待审核  
**文档目的**：同步已确认的需求与构建思路，征询意见，可用于其他 Agent / 工具的能力测试

---

## 一、项目背景与目标

### 1.1 背景

本项目面向压延膜制造企业，基于已有的本地企业知识库（产品文档、工艺流程、车间管理类文档），构建一套**2D 三元组知识图谱可视化 + 深度融合智能问答**系统。

现有资产：
- 本地原生 OpenClaw 已部署
- enterprise-rag 等 Skill 已部署，部分文档已切片向量化入库
- 本地 Ollama（NVIDIA 8G 显存）已部署
- 云端 GLM 最新模型可用
- 本地 Python / WSL 环境可灵活配置

### 1.2 目标

- **核心目标**：跑通从"文档 → 三元组抽取 → 图谱存储 → 可视化展示 → 深度融合问答"的完整闭环
- **定位**：个人测试学习 + DEMO 展示，非生产级系统
- **未来扩展**：验证可行后，封装为 OpenClaw Skill / MCP Server，融入 Agent 生态

---

## 二、需求确认清单

| 维度 | 确认结果 | 备注 |
|------|---------|------|
| 使用场景 | 个人测试学习，DEMO 展示 | 核心目标是跑通闭环 |
| 数据规模 | 100+ 篇 Word/MD，约几百 MB | 未来持续增量更新 |
| 内容领域 | 压延膜制造业：产品文档 + 工艺流程 + 车间管理 | 企业普通类文档暂不纳入 |
| 结构化数据 | 无现成 BOM/工艺表 | 全部从非结构化文档抽取，本体按压延膜业界标准定义 |
| 运行环境 | 本地单机，Windows + WSL 均可 | 8G 显存 NVIDIA，Ollama 已部署，云端 GLM 可用 |
| 图存储方案 | NetworkX 内存图 + SQLite 持久化 | 暂不上 Neo4j；数据量破 5 万节点时考虑迁移 |
| 可视化方案 | 方案 B：ECharts 可交互网页 | 支持拖拽、缩放、搜索定位、点击展开邻接节点 |
| 问答深度 | 档次 3：图谱与 RAG 深度融合 | 图谱作为结构化推理证据，非仅展示 |
| 部署形态 | 路线 A：独立 FastAPI Web 应用先跑通 | 路线 B（OpenClaw 集成）作为未来阶段 |
| LLM 分工 | 云端 GLM 做批量三元组抽取；本地 Ollama 7B 做实时问答 | 抽取质量优先，问答低延迟优先 |

---

## 三、整体技术架构

### 3.1 架构总览

```
┌──────────────────────────────────────────────────────────┐
│  前端（单页 HTML + ECharts）                              │
│  ┌──────────────┐  ┌──────────────┐                      │
│  │  知识图谱区   │  │  问答对话区   │                      │
│  │  ECharts力导向│  │  搜索框+答案  │                      │
│  │  搜索/高亮/展开│  │  溯源+来源标注│                      │
│  └──────┬───────┘  └──────┬───────┘                      │
└─────────┼─────────────────┼──────────────────────────────┘
          │  JSON           │  JSON
┌─────────┴─────────────────┴──────────────────────────────┐
│  后端（FastAPI）                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐   │
│  │ /graph   │ │ /search  │ │ /qa      │ │ /admin     │   │
│  │ 子图/路径 │ │ 实体搜索 │ │ 融合问答  │ │ 抽取/管理  │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └─────┬──────┘   │
│       │             │            │              │          │
│  ┌────┴─────────────┴────────────┴──────────────┴──────┐  │
│  │              核心服务层                                │  │
│  │  GraphService(NetworkX)  RAGService(enterprise-rag)   │  │
│  │  EntityLinker          QAPipeline(图谱+RAG融合)       │  │
│  └────┬──────────────┬──────────────┬───────────────────┘  │
└───────┼──────────────┼──────────────┼──────────────────────┘
        │              │              │
  ┌─────┴────┐  ┌──────┴─────┐  ┌────┴──────────┐
  │ SQLite   │  │ FAISS/     │  │ Ollama(本地)   │
  │ 三元组/  │  │ Chroma     │  │ 7B 实时问答     │
  │ 文档元数据│  │ 向量库     │  │                │
  └──────────┘  └────────────┘  └────────────────┘
                                       ↑
                                ┌──────┴───────┐
                                │ 云端 GLM      │
                                │ 批量三元组抽取 │
                                └──────────────┘
```

### 3.2 问答流程（档次 3 深度融合）

```
用户提问
  ↓
① 实体链接：从问题中识别实体，匹配到图谱节点
  ↓
② 图谱检索：取相关子图 / 关系路径（精确结构化事实）
  ↓
③ RAG 检索：enterprise-rag 向量检索相关文档片段（模糊描述性内容）
  ↓
④ 融合 Prompt：图谱三元组 + 文档片段 一起喂给 LLM
  ↓
⑤ 生成答案：明确标注"来自图谱"vs"来自文档"
  ↓
⑥ 前端联动：图谱中高亮相关节点和路径
```

---

## 四、本体 Schema 定义（压延膜行业标准）

### 4.1 实体类型（11 类）

| 类型 | 说明 | 示例 |
|------|------|------|
| 产品 | 最终产出的膜产品 | PVC 压延膜 0.3mm 透明 |
| 原材料 | 配方原料 | PVC 树脂、DOTP 增塑剂、钙锌稳定剂 |
| 配方 | 产品对应的原料配比 | 透明膜配方 A |
| 工艺/产线 | 整体工艺流程 | 压延成型工艺、挤出压延线 |
| 工序 | 工艺中的具体步骤 | 配料、密炼、开炼、压延、冷却、收卷 |
| 设备 | 生产设备 | 密炼机、四辊压延机、冷却辊组 |
| 车间/产线 | 物理位置 | 压延车间、一号压延线 |
| 工艺参数 | 可量化的工艺设定 | 辊温 180℃、辊速 25m/min、辊隙 0.3mm |
| 质量指标 | 产品检验标准 | 厚度公差 ±5%、拉伸强度 ≥20MPa |
| 缺陷/异常 | 生产中的问题 | 厚度不均、晶点、气泡、色差 |
| 岗位 | 人员角色 | 压延主操、配料工、质检员 |

### 4.2 关系类型（核心 8 类）

| 关系 | 方向 | 示例 |
|------|------|------|
| 由组成 | 产品 → 原材料/零部件 | 透明膜 - 由组成 → PVC树脂 |
| 遵循工艺 | 产品 → 工艺 | 透明膜 - 遵循工艺 → 压延成型工艺 |
| 包含工序 | 工艺 → 工序 | 压延工艺 - 包含工序 → 压延成型 |
| 使用设备 | 工序 → 设备 | 压延成型 - 使用设备 → 四辊压延机 |
| 位于 | 设备 → 车间 | 压延机 - 位于 → 压延车间 |
| 设定参数 | 工序 → 工艺参数 | 压延成型 - 设定参数 → 辊温180℃ |
| 检验指标 | 产品/工序 → 质量指标 | 透明膜 - 检验指标 → 厚度公差 |
| 产生缺陷 | 工序/参数 → 缺陷 | 辊温过低 - 产生缺陷 → 晶点 |

### 4.3 核心工艺主线

**配方 → 配料 → 密炼 → 开炼 → 过滤 → 压延成型 → 冷却定型 → 测厚 → 收卷 → 分切 → 质检**

---

## 五、分阶段实施计划

| 阶段 | 名称 | 核心内容 | 产出 | 复杂度 |
|------|------|---------|------|--------|
| **P0** | 环境勘察与项目初始化 | 确认 Python 环境、目录结构、依赖安装、连通性测试（Ollama/GLM/enterprise-rag） | 可运行的空壳 FastAPI 项目 + 环境报告 | 低 |
| **P1** | 数据层与三元组抽取 | 文档解析 → 云端 GLM 批量抽取三元组 → 实体消歧 → 存入 NetworkX + SQLite | 图数据库 + 抽取质量报告 | 中 |
| **P2** | 图谱可视化层 | NetworkX 布局计算 → ECharts 交互前端 → 搜索/高亮/点击展开 | 可交互的 2D 图谱网页 | 中 |
| **P3** | 深度融合问答层 | 实体链接 → 图谱子图检索 → RAG 检索 → 融合 Prompt → 本地 LLM 生成 → 溯源联动 | 智能问答页面 + 图谱联动 | 中高 |
| **P4** | 闭环优化与文档 | 增量更新机制 → 性能优化 → 测试用例 → 部署手册 | 完整可运行系统 + 存档文档 | 中 |

> 每阶段完成后暂停，展示效果，确认后再进入下一阶段。

---

## 六、各阶段详细方案

### P0：环境勘察与项目初始化

**目标**：搭好项目骨架，确认所有外部依赖连通。

**执行步骤**：
1. 检查 Python 版本（推荐 3.10 / 3.11，避开 3.14 依赖冲突）
2. 检查 Ollama 服务状态和可用模型
3. 确认 enterprise-rag 的向量库位置和嵌入模型
4. 确认云端 GLM API 可调用
5. 创建项目目录结构
6. 安装依赖（fastapi, uvicorn, networkx, matplotlib, python-docx, markdown, sqlalchemy, requests, pydantic, jinja2）
7. 写 `/health` 接口，返回各依赖状态
8. 最小前端页面验证
9. 生成环境报告（MD 存档）

**项目目录结构**：
```
kg_qa_system/
├── app/
│   ├── main.py              # FastAPI 入口
│   ├── routers/             # API 路由
│   │   ├── graph.py
│   │   ├── search.py
│   │   ├── qa.py
│   │   └── admin.py
│   ├── services/            # 核心服务
│   │   ├── graph_service.py
│   │   ├── rag_service.py
│   │   ├── entity_linker.py
│   │   └── qa_pipeline.py
│   ├── models/              # 数据模型
│   └── static/              # 前端静态文件
│       ├── index.html
│       ├── css/
│       └── js/
├── data/
│   ├── docs/                # 原始文档
│   ├── triples/             # 抽取的三元组
│   └── vector_store/        # 向量库
├── db/
│   └── knowledge_graph.db   # SQLite 数据库
├── config/
│   └── settings.py          # 配置
├── scripts/
│   ├── extract_triples.py   # 批量抽取脚本
│   └── init_db.py           # 数据库初始化
├── requirements.txt
└── README.md
```

**完成标准**：
- `uvicorn` 启动后浏览器能打开页面
- `/health` 接口返回所有依赖状态正常
- 项目目录结构完整

---

### P1：数据层与三元组抽取

**目标**：从文档中抽取三元组，构建知识图谱。

**执行步骤**：
1. 文档解析：`python-docx` 读 Word，直接读 MD，按章节/段落切分
2. 设计抽取 Prompt：基于已确认的本体 Schema， instruct 云端 GLM 抽取指定类型的实体和关系
3. 批量抽取：逐文档/逐段落调用 GLM，结果缓存为 JSON（支持断点续跑）
4. 实体消歧：同名实体合并、简称/全称对齐、类型校验
5. 存入 NetworkX：构建有向图，节点带类型属性，边带关系类型和来源文档属性
6. 持久化到 SQLite：三元组表、实体表、文档元数据表、抽取日志表
7. 抽取质量报告：统计实体数量、关系数量、各类型分布、疑似错误样例

**关键设计**：
- 抽取结果必须包含 `source_doc` 和 `source_section`，用于问答溯源
- 批量抽取支持断点续跑（已抽取的段落跳过）
- 实体消歧规则：同类型 + 名称相似度 > 阈值 → 合并；保留别名

**完成标准**：
- 100+ 篇文档全部处理完成
- NetworkX 图可加载，节点/边数量合理
- SQLite 中三元组可查询
- 质量报告输出

---

### P2：图谱可视化层

**目标**：ECharts 可交互 2D 图谱网页。

**执行步骤**：
1. 后端 `/graph` 接口：返回全图或子图的 JSON（节点含 id/name/type/symbolSize，边含 source/target/relation）
2. NetworkX 布局计算：spring_layout 计算初始坐标，返回给前端
3. 前端 ECharts 力导向图：
   - 按实体类型着色（11 种颜色，图例可切换显隐）
   - 支持拖拽节点、缩放画布
   - 搜索框：输入实体名 → 高亮定位该节点
   - 点击节点：展开/折叠邻接节点（默认显示 2 度关系）
   - 鼠标悬停：显示节点详情（类型、关联文档、关系数量）
4. 后端 `/search` 接口：实体模糊搜索，返回匹配节点列表
5. 性能优化：节点超过 1000 时默认只显示中心子图，按需加载

**完成标准**：
- 浏览器打开可看到完整图谱
- 搜索、点击展开、拖拽缩放均正常
- 不同实体类型颜色区分清晰

---

### P3：深度融合问答层

**目标**：图谱 + RAG 融合的智能问答，与图谱联动。

**执行步骤**：
1. 实体链接（EntityLinker）：
   - 从用户问题中提取候选实体
   - 与图谱节点做模糊匹配（名称 + 类型）
   - 返回匹配到的节点 ID 列表
2. 图谱检索（GraphService）：
   - 按匹配节点取 1-2 度子图
   - 支持两节点间路径查询（最短路径 / 所有路径）
   - 返回三元组列表（带关系方向）
3. RAG 检索（RAGService）：
   - 复用 enterprise-rag 的向量库和检索逻辑
   - 返回 Top-K 相关文档片段（带来源和相似度）
4. 融合 Prompt 设计：
   - 系统提示：区分"结构化事实（来自图谱）"和"参考文档（来自知识库）"
   - instruct 模型优先使用图谱事实，文档片段作为补充
   - 要求答案中标注信息来源
5. 本地 Ollama 生成：调用 7B 模型生成答案
6. 答案后处理：提取答案中涉及的实体 ID，返回给前端用于图谱高亮
7. 前端问答区：
   - 对话式界面（历史记录）
   - 答案中"来自图谱"和"来自文档"用不同样式标注
   - 答案中的实体名可点击 → 图谱自动定位高亮
   - 引用来源可展开查看原文片段

**关键难点**：
- 实体链接的准确率（问题表述 vs 图谱节点名称的差异）
- 图谱结果和 RAG 结果的融合策略（权重、去重、冲突处理）
- Prompt 工程：让 LLM 正确区分并利用两类信息

**完成标准**：
- 输入问题能返回答案 + 来源标注
- 图谱中相关节点自动高亮
- 至少 5 个测试问题验证准确率

---

### P4：闭环优化与文档

**目标**：完善增量更新、性能优化、测试用例、存档文档。

**执行步骤**：
1. 增量更新机制：
   - 监控文档目录，新文档自动触发抽取
   - 已有文档更新时，删除旧三元组后重新抽取
   - 实体合并冲突处理
2. 性能优化：
   - 图谱布局缓存（避免每次重新计算）
   - 问答结果缓存（相同问题直接返回）
   - 子图查询索引优化
3. 测试用例：
   - 抽取质量测试（抽样人工校验）
   - 问答准确率测试（20+ 标准问题集）
   - 边界测试（空结果、模糊问题、跨领域问题）
4. 文档输出：
   - 部署手册（MD / Word）
   - API 接口文档
   - 本体 Schema 说明
   - DEMO 操作指南

**完成标准**：
- 新增文档可自动入库并更新图谱
- 问答响应时间 < 10 秒（本地 LLM）
- 测试用例通过率达标
- 完整存档文档交付

---

## 七、风险与待确认事项

### 7.1 已识别风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 三元组抽取质量不稳定 | 图谱噪音多，问答准确率下降 | 抽样人工校验 + 消歧规则 + 增量迭代优化 Prompt |
| 8G 显存运行 7B 模型紧张 | 问答时可能 OOM 或速度慢 | 使用 Q4_K_M 量化版；抽取用云端 GLM 不占本地显存 |
| 实体链接准确率不足 | 问答时找不到正确图谱节点 | 模糊匹配 + 别名表 + 人工维护常见问题的实体映射 |
| NetworkX 大图性能瓶颈 | 节点过多时布局计算和查询慢 | 子图按需加载 + 布局缓存；5 万节点以上迁移 Neo4j |
| enterprise-rag 向量库兼容性 | 嵌入模型不一致导致检索失效 | P0 阶段确认嵌入模型，保持一致 |

### 7.2 待确认事项（执行 P0 前需明确）

1. **项目目录位置**：是否使用 `C:\Users\Admin\Doubao\projects\kg_qa_system\`？
2. **Python 环境**：Windows 原生 Python 还是 WSL 内 Python？（推荐 Windows 原生）
3. **Python 版本**：当前可用版本是多少？是否需要新建 venv？
4. **enterprise-rag 向量库**：具体路径和嵌入模型型号？
5. **云端 GLM**：API 调用方式和 Key 是否已配置？
6. **本地 Ollama 模型**：当前已拉取哪些模型？问答用哪个？

---

## 八、审核意见征集

请审核以下方面并反馈：

1. **需求完整性**：是否有遗漏的关键需求或场景？
2. **技术选型**：NetworkX + SQLite + FastAPI + ECharts 的组合是否合理？有无更优方案？
3. **本体 Schema**：11 类实体 + 8 类关系是否覆盖压延膜行业核心知识？有无需要增减？
4. **问答融合策略**：图谱优先 + RAG 补充的策略是否合理？有无其他融合思路？
5. **分阶段计划**：阶段划分是否合理？有无需要调整优先级？
6. **风险评估**：是否有未识别的重大风险？
7. **其他建议**：任何改进意见或替代方案

---

*本文档可直接转发给其他 Agent 或工具进行能力测试与方案评审。*
